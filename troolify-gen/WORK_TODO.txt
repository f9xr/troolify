TROOLIFY - BATCH 2 WORK TODOS
==============================
Repo: C:\Users\SKYLIMIT OFFICE\Documents\GitHub\troolify
Generated: 2026-09-22
Build:           node troolify-gen/minify.js   (idempotent, safe to re-run)
Env notes:       PowerShell mangles \" and \w inline; put node scripts in temp .js files.
                 html-minifier-terser must be required from repo root.

DONE
----
[X] Font Awesome self-hosted + subset
      assets/fonts/fa-solid.min.woff2 (318 glyphs)
      assets/fonts/fa-brands.min.woff2 (10 glyphs, includes fa-markdown fix)
      assets/css/icons.min.css (326 solid + 10 brands rules)
      Verifier: ALL 336 rules present, NONE missing (verify_icons.py)
[X] Google Fonts self-hosted
      assets/fonts/jakarta-{400,500,600,700,800}.woff2
      assets/css/jakarta.min.css
      All 343 pages swapped to local fonts
[X] HTML minify pass (html-minifier-terser, conservative opts)
      All 344 pages marked <!-- data-minimized --> (re-run = 0)
      304 utteranc placeholders intact
      Fixed invalid HTML: tools/coding/jsonpath-tester.html,
                          tools/pdf/pdf-metadata-viewer.html

IN PROGRESS / TODO
-----------------
[X] ASSETS/JS/TOOLS-DATA SLIM SPLIT  (done 2026-09-23)
    Design (decided):
      - NEW assets/js/tools-data-slim.js sets window.TOOLS_SLIM = [render fields]
        (name, desc, icon, tag, category, href) + window.CATEGORIES.
      - window.TOOLS stays FULL source of truth, lazy-loaded as today
        (layout.js loadToolsData / loadToolsDataForSidebar, tool-page.js,
        dashboard-nav.js all read window.TOOLS - no change needed there).
    Steps:
      1. troolify-gen/gen-slim.js generates tools-data-slim.js from tools-data.js
         (strips keywords + workflow). minify.js regenerates it each build.
      2. home-catalog.js:17 -> var tools = window.TOOLS_SLIM || window.TOOLS || [];
      3. catalog.js reads window.TOOLS_FULL || window.TOOLS_SLIM || window.TOOLS;
         keyword search lazy-injects tools-data.min.js on first keystroke (shared
         window.__TOOLS_PENDING guard), then re-renders with keywords.
      4. minify.js swaps the eager tools-data include -> tools-data-slim.min.js
         on the 27 hub pages (index.html, tools/index.html, 25 category indexes).
      5. Verified: 2-run idempotent, hub pages slim / leaf lazy-loaders full,
         slim lacks keywords+workflow (304 tools / 25 cats, 87KB min vs 150KB).

[X] LAZY HOMEPAGE BADGES + CDNJS PRECONNECT  (done 2026-09-23)
      - loading="lazy" added to nicklaunches (2x), stork.ai (2x), findyoursaas (2x).
      - minify.js adds cdnjs preconnect to the 15 leaf pages that load cdnjs libs
        (css/js-beautifier, qr, pdf tools). Collision with legacy FA-preconnect
        remover fixed (it only strips now when the FA stylesheet is swapped).

[~] HERO IMAGE RECOMPRESS - SKIPPED (no cwebp/magick/ffmpeg or sharp installed;
      images already WebP: hero1 51KB, hero2 81KB, hero3 104KB, og 39KB).
      FINAL VERIFY done: run minify.js twice (0 changes), spot-check
      hub/leaf script tags, 0 remaining googleapis / cdnjs font-awesome refs.

[X] ROUND-2 PERF PASS  (done 2026-09-23)   <-501KB sitewide vs HEAD (uncommitted)
      - Homepage: removed dead seo-article.min.css (no .seo-article markup) -14.4KB
        render-blocking, and dropped code.market/widget.min.js external script.
      - Inline JSON-LD blocks compacted (JSON-safe) -73KB sitewide.
      - Inline tool-logic <script> blocks terser-minified to fixed-point
        (idempotent; JSON-LD excluded). ~2.6KB/page avg transfer+parse saved.
      - seo-article.min.css now preload + media="print" onload (noscript twin) -
        out of the render-blocking chain on all 304 tool pages. Lookbehind guard
        prevents re-wrapping the noscript link (idempotent).
      - NOTE: f06715282b6043069eef9926d2c8e12b.txt is the IndexNow API key --
        DO NOT DELETE.
      - Verify: minify.js twice -> run 2 = 0 changes; node --check clean.

VERIFY SCRIPTS (known good)
---------------------------
- Icons verifier:     %TEMP%\opencode\verify_icons.py   (checks pc in (sk | bk))
- Structural check:   walk .html, count data-minimized markers + data-utteranc
- node --check assets\js\<file>.js after every edit