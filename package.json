{
  "name": "troolify",
  "version": "1.0.0",
  "private": true,
  "description": "Troolify - static tool hub build tooling (minify + asset reference rewrites)",
  "scripts": {
    "minify": "node troolify-gen/minify.js"
  },
  "devDependencies": {
    "clean-css": "^5.3.2",
    "html-minifier-terser": "^7.2.0",
    "terser": "^5.30.0"
  }
}
